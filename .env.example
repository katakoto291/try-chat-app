# Claude API キー。未設定なら「モックモード」（API を呼ばずに動作を模擬）で起動します。
ANTHROPIC_API_KEY=

# mock | anthropic （未指定なら API キーの有無で自動判定）
LLM_MODE=

# 使用モデル（省略時は claude-opus-5）
ORCHESTRATOR_MODEL=claude-opus-5
SUBAGENT_MODEL=claude-opus-5

PORT=3000
